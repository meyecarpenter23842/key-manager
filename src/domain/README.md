# Domain layer

Business invariants for applications, licenses, customers and devices live here.

Route handlers and UI components must call domain/services instead of duplicating license rules. Phase 4 and Phase 5 will add the concrete domain implementation.
